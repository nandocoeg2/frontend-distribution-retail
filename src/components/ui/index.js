// UI Components Barrel Export
// This file provides a centralized export for all reusable UI components

// Accordion Components
export { 
  Accordion, 
  AccordionItem, 
  AccordionSection // for backward compatibility
} from './Accordion';

// Tab Components
export { 
  TabContainer, 
  Tab, 
  TabContent, 
  TabPanel, 
  TabNavigation // for backward compatibility
} from './Tabs';

// Badge Components
export { 
  Badge, 
  StatusBadge, 
  NumberBadge, 
  IconBadge, 
  DotBadge 
} from './Badge';

// Card Components
export { 
  Card, 
  InfoCard, 
  StatCard, 
  FeatureCard, 
  CardHeader, 
  CardFooter 
} from './Card';

// Loading Components
export { 
  LoadingState, 
  Spinner, 
  LoadingDots, 
  ProgressBar, 
  CircularProgress, 
  Skeleton, 
  PageLoading, 
  CardLoading, 
  TableLoading 
} from './Loading';

// Button Components (if created in the future)
// export { Button, IconButton, ButtonGroup } from './Button';

// Input Components (if created in the future)
// export { Input, TextArea, Select, Checkbox, Radio } from './Input';

// Modal Components (if created in the future)
// export { Modal, Dialog, Drawer } from './Modal';

// Alert Components (if created in the future)
// export { Alert, Toast, Notification } from './Alert';